<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sp.system.mapper.WecomSchoolDepartmentMapper">

    <resultMap id="WecomSchoolDepartmentResultMap" type="com.sp.system.entity.WecomSchoolDepartment">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="nameEn" column="name_en"/>
        <result property="order" column="order_num"/>
        <result property="departmentLeader" column="department_leader"/>
    </resultMap>

    <insert id="batchInsertSchoolDepartments" parameterType="java.util.List">
        INSERT INTO wecom_school_department (id, parent_id, name, name_en, order_num, department_leader)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.parentId}, #{item.name}, #{item.nameEn}, #{item.order},
            #{item.departmentLeader})
        </foreach>
        ON DUPLICATE KEY UPDATE
        parent_id = VALUES(parent_id),
        name = VALUES(name),
        name_en = VALUES(name_en),
        order_num = VALUES(order_num),
        department_leader = VALUES(department_leader)
    </insert>

    <select id="selectAll" resultMap="WecomSchoolDepartmentResultMap">
        SELECT id,
               parent_id,
               name,
               name_en,
               order_num,
               department_leader
        FROM wecom_school_department
        ORDER BY parent_id, order_num
    </select>

    <update id="updateSchoolDepartment" parameterType="com.sp.system.entity.WecomSchoolDepartment">
        UPDATE wecom_school_department
        SET parent_id = #{parentId},
            name = #{name},
            name_en = #{nameEn},
            order_num = #{order},
            department_leader = #{departmentLeader}
        WHERE id = #{id}
    </update>

    <delete id="deleteSchoolDepartmentById" parameterType="java.lang.Long">
        DELETE FROM wecom_school_department
        WHERE id = #{id}
    </delete>

</mapper>
