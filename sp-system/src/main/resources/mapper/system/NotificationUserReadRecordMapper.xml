<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.system.mapper.NotificationUserReadRecordMapper">

    <resultMap id="ReadRecordResult" type="com.sp.system.entity.NotificationUserReadRecord">
        <id     property="readId"       column="read_id"/>
        <result property="sendRecordId" column="send_record_id"/>
        <result property="userId"       column="user_id"/>
        <result property="userType"     column="user_type"/>
        <result property="isRead"       column="is_read"/>
        <result property="readTime"     column="read_time"/>
        <result property="replyStatus"  column="reply_status"/>
        <result property="replyTime"    column="reply_time"/>
        <result property="sendStatus"   column="send_status"/>
        <result property="createTime"   column="create_time"/>
    </resultMap>

    <!-- 按 notification_id + user_id + student_user_id 聯查 -->
    <select id="selectByNotificationAndUser" resultMap="ReadRecordResult">
        SELECT r.read_id, r.send_record_id, r.user_id, r.user_type,
               r.is_read, r.read_time, r.reply_status, r.reply_time, r.send_status, r.create_time
        FROM notification_user_read_record r
        INNER JOIN notification_send_record s ON s.send_record_id = r.send_record_id
        INNER JOIN notification n ON n.notification_id = s.notification_id AND n.status = '1'
        WHERE s.notification_id = #{notificationId}
          AND r.user_id = #{userId}
          AND r.send_status = '1'
        <if test="studentUserId != null and studentUserId != ''">
          AND r.student_user_id = #{studentUserId}
        </if>
        LIMIT 1
    </select>

    <!-- 標記爲已讀 -->
    <update id="markAsRead">
        UPDATE notification_user_read_record
        SET is_read   = '1',
            read_time = #{readTime}
        WHERE read_id = #{readId}
          AND is_read = '0'
    </update>

    <!-- 標記爲已回復 -->
    <update id="markAsReplied">
        UPDATE notification_user_read_record
        SET reply_status = '1',
            reply_time   = #{replyTime}
        WHERE read_id = #{readId}
          AND reply_status = '0'
    </update>

    <!-- 通知含閱讀狀態的 ResultMap（以 notification_user_read_record 爲驅動） -->
    <resultMap id="NotificationWithReadStatusResult"
               type="com.sp.system.entity.vo.NotificationWithReadStatusVO">
        <id     property="notificationId"  column="notification_id"/>
        <result property="title"           column="title"/>
        <result property="content"         column="content"/>
        <result property="senderId"        column="sender_id"/>
        <result property="senderName"      column="sender_name"/>
        <result property="jumpUrl"         column="jump_url"/>
        <result property="attachmentUrls"  column="attachment_urls"/>
        <result property="status"          column="status"/>
        <result property="replyDeadline"   column="reply_deadline"/>
        <result property="createBy"        column="create_by"/>
        <result property="createTime"      column="create_time"/>
        <result property="updateBy"        column="update_by"/>
        <result property="updateTime"      column="update_time"/>
        <result property="remark"          column="remark"/>
        <result property="readId"          column="read_id"/>
        <result property="isRead"          column="is_read"/>
        <result property="sendRecordId"    column="send_record_id"/>
        <result property="sendStatus"      column="send_status"/>
    </resultMap>

    <!--
        分頁查詢已發布通知（僅顯示發送給當前用戶的通知，附帶閱讀狀態）
        以 notification_user_read_record 爲驅動表：
          - INNER JOIN notification_send_record 確認發送記錄存在
          - INNER JOIN notification 過濾狀態爲已發布（status='1'）
        無 GROUP BY，無聚合，完全兼容 only_full_group_by 模式。
    -->
    <select id="selectPublishedNotificationsForUser"
            resultMap="NotificationWithReadStatusResult">
        SELECT n.notification_id, n.title, n.content, n.sender_id, n.sender_name,
               n.jump_url, n.attachment_urls, n.status, n.reply_deadline,
               n.create_by, n.create_time, n.update_by, n.update_time, n.remark,
               r.read_id, r.is_read, r.send_record_id, r.send_status
        FROM notification_user_read_record r
        INNER JOIN notification_send_record s ON s.send_record_id = r.send_record_id
        INNER JOIN notification n
               ON n.notification_id = s.notification_id
              AND n.status = '1'
        WHERE r.user_id = #{userId}
          AND r.send_status = '1'
        <if test="studentUserId != null and studentUserId != ''">
          AND r.student_user_id = #{studentUserId}
        </if>
        ORDER BY n.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 已發布通知總數（僅統計發送給當前用戶的通知） -->
    <select id="countPublishedNotificationsForUser" resultType="int">
        SELECT COUNT(*)
        FROM notification_user_read_record r
        INNER JOIN notification_send_record s ON s.send_record_id = r.send_record_id
        INNER JOIN notification n
               ON n.notification_id = s.notification_id
              AND n.status = '1'
        WHERE r.user_id = #{userId}
          AND r.send_status = '1'
        <if test="studentUserId != null and studentUserId != ''">
          AND r.student_user_id = #{studentUserId}
        </if>
    </select>

    <!-- 未讀通知數量（僅統計發送給當前用戶的未讀通知） -->
    <select id="countUnreadNotificationsForUser" resultType="int">
        SELECT COUNT(*)
        FROM notification_user_read_record r
        INNER JOIN notification_send_record s ON s.send_record_id = r.send_record_id
        INNER JOIN notification n ON n.notification_id = s.notification_id AND n.status = '1'
        WHERE r.user_id = #{userId}
          AND r.send_status = '1'
          AND (r.is_read = '0' OR r.is_read IS NULL)
        <if test="studentUserId != null and studentUserId != ''">
          AND r.student_user_id = #{studentUserId}
        </if>
    </select>

</mapper>
